<div class="container text-center">
  <h2 class="my-4 text-success">
    <strong><%= @task.title %></strong>
    <span class="text-warning">🏉</span>
  </h2>

  <p class="text-dark"><strong>Details:</strong> <%= @task.details %></p>
  <p class="text-dark"><strong>Completed:</strong> <%= @task.completed ? "Yes" : "No" %></p>

  <div class="d-flex justify-content-center mt-4 gap-3">
    <%= link_to "Back to task list", tasks_path, class: "btn btn-outline-success btn-lg" %>
    <%= link_to "Edit the task", edit_task_path, class: "btn btn-outline-primary btn-lg" %>
    <%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { turbo_confirm: "Are you sure?", turbo_method: :delete },
      class: "btn btn-outline-danger btn-lg" %>
  </div>

  <p class="mt-4 text-muted">
    <small>Task details page - Play hard, work hard! 🏉</small>
  </p>

  <!-- Ajouter un ballon de rugby centré en bas -->
  <div class="position-fixed bottom-0 start-50 translate-middle-x p-3">
    <span class="text-success" style="font-size: 2rem;">🏉</span>
  </div>
</div>
